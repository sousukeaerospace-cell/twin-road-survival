# 『ツイン・ロード・サバイバル：ラスト・ディフェンス』 プロトタイプ作成ガイド

> **ジャンル:** リバース・タワーディフェンス ＋ レーン防衛アクション
> **視点:** クォータービュー（崩壊した都市の高速道路）
> **推奨エンジン:** Python / Pygame（本ガイドのメイン）
> **代替エンジン:** Unity / C#（各フェーズ末に補足）

---

## 0. 前提と本ガイドの使い方

本ガイドは7つのフェーズに分かれており、各フェーズを順番に進めることでプロトタイプが完成します。各フェーズには「ゴール → 実装手順 → 確認チェックリスト → 所要時間」を記載しています。フェーズ1～3まで完成すればコアループの手触りを確認でき、フェーズ4まで進めればデモ可能な状態になります。

---

## 1. 開発環境のセットアップ

### 1-1. Python / Pygame（推奨）

プロトタイプの素早い立ち上げにはPython + Pygameが最適です。グラフィック描画・入力処理・サウンド再生が一通り揃っており、学習コストも低いためです。

```bash
# Python 3.10以上を推奨
python --version

# 仮想環境を作成
python -m venv trs_env
source trs_env/bin/activate   # Windows: trs_env\Scripts\activate

# Pygame のインストール
pip install pygame-ce          # Community Edition（最新機能対応）

# 補助ライブラリ
pip install pytmx              # Tiledマップ読み込み（任意）
pip install pillow             # 画像処理ユーティリティ（任意）
```

**エディタ:** VS Code + Python拡張機能を推奨。`launch.json`にPygameのデバッグ構成を追加しておくと便利です。

### 1-2. Unity / C#（代替案）

将来的にモバイルリリースを見据える場合はUnityが有力です。ただしプロトタイプ速度はPygameに劣ります。

- Unity 2022 LTS 以上を使用
- 2D URP テンプレートで新規プロジェクトを作成
- 必須パッケージ: `2D Sprite`, `2D Tilemap`, `TextMeshPro`, `Input System`
- クォータービュー実装には `Isometric Tilemap` を使用

---

## 2. プロジェクト構成

### 2-1. Pygame 版フォルダ構造

```
twin_road_survival/
├── main.py                  # エントリーポイント
├── config.py                # 定数・設定値（画面サイズ、FPS等）
├── game/
│   ├── __init__.py
│   ├── scene_manager.py     # シーン管理（タイトル → ゲーム → リザルト）
│   ├── game_scene.py        # メインゲームシーン
│   ├── lane.py              # レーンクラス（左右2車線）
│   ├── commander.py         # 指揮官（プレイヤーキャラ）
│   ├── soldier.py           # 兵士ユニット（自動攻撃）
│   ├── enemy.py             # 敵ユニット
│   ├── boss.py              # ボスユニット
│   ├── item.py              # アイテム基底クラス
│   ├── items/
│   │   ├── portal.py        # 増強のポータル
│   │   ├── attack_field.py  # 攻撃の陣
│   │   ├── heavy_weapon.py  # 重火器キット
│   │   ├── airstrike.py     # 爆撃要請
│   │   ├── medpack.py       # メディカルパック
│   │   └── shield.py        # エナジーシールド
│   ├── wave.py              # ウェーブ管理
│   ├── upgrade.py           # アップグレードシステム
│   └── camera.py            # クォータービュー用カメラ
├── ui/
│   ├── hud.py               # HP、コイン、ウェーブ表示
│   ├── item_bar.py          # アイテムスロットUI
│   └── result_screen.py     # 勝敗画面
├── effects/
│   ├── particle.py          # パーティクルエフェクト
│   └── screen_shake.py      # 画面揺れ
├── assets/
│   ├── sprites/             # キャラクター・背景画像
│   ├── sounds/              # SE・BGM
│   ├── fonts/               # フォントファイル
│   └── data/
│       ├── waves.json       # ウェーブ定義データ
│       ├── enemies.json     # 敵パラメータ
│       └── items.json       # アイテムパラメータ
├── tests/
│   ├── test_lane.py
│   ├── test_combat.py
│   └── test_wave.py
└── README.md
```

### 2-2. config.py の初期設定例

```python
# config.py
SCREEN_WIDTH = 1280
SCREEN_HEIGHT = 720
FPS = 60
TILE_WIDTH = 64
TILE_HEIGHT = 32    # クォータービューの菱形タイル

# レーン設定
LANE_COUNT = 2
LANE_Y_POSITIONS = [300, 480]  # 画面上での各レーンのY座標

# ゲームバランス（仮値 — Phase 7 で調整）
COMMANDER_MAX_HP = 100
SOLDIER_ATTACK_INTERVAL = 1.0   # 秒
SOLDIER_DAMAGE = 10
ENEMY_BASE_HP = 30
ENEMY_SPEED = 50                # px/sec
COIN_PER_KILL = 10
```

---

## 3. Phase 1: 基本レーン＆キャラクター表示

**ゴール:** 2車線の道路が描画され、指揮官と兵士が表示される状態。レーン切り替え操作が機能する。

### 手順

**Step 1-1: ゲームウィンドウの立ち上げ**

`main.py`にPygameの初期化処理とゲームループの骨格を実装します。`pygame.time.Clock`でFPS制御し、`config.py`の定数を参照する構成にします。

**Step 1-2: クォータービュー座標系の実装**

`camera.py`にワールド座標からスクリーン座標への変換関数を作成します。プロトタイプ段階では厳密なアイソメトリック投影でなくても構いません。道路のパースを斜めに描画し、遠近感を表現する程度で十分です。

```python
def world_to_screen(wx, wy):
    sx = (wx - wy) * (TILE_WIDTH // 2) + SCREEN_WIDTH // 2
    sy = (wx + wy) * (TILE_HEIGHT // 2) + 100
    return sx, sy
```

**Step 1-3: レーンの描画**

`lane.py`で左右2レーンを定義します。各レーンは敵の進軍経路（ウェイポイントのリスト）と、兵士の配置スロットを持ちます。プロトタイプでは色分けした矩形でレーンを表示すれば十分です。

**Step 1-4: 指揮官の表示とレーン切り替え**

`commander.py`で指揮官キャラクターを実装します。キーボードの左右矢印キー（またはA/D）でアクティブレーンを切り替えます。切り替え時にスムーズなスライドアニメーション（lerp補間）を入れると操作感が良くなります。

**Step 1-5: 兵士の配置と表示**

`soldier.py`で兵士ユニットを実装します。各レーンに初期配置される兵士は3～5体。この段階では立っているだけで攻撃はしません。

### 確認チェックリスト

- [ ] ゲームウィンドウが指定サイズで開く
- [ ] 2つのレーンが視覚的に区別できる
- [ ] 指揮官が左右レーン間を移動できる
- [ ] 各レーンに兵士が表示されている
- [ ] FPSが安定している（60fps）

### 所要時間目安: 2～3日

### Unity代替メモ

Tilemapで`Isometric`グリッドを設定し、レーンをTilemap上の2列として定義します。指揮官の移動は`DOTween`を使うとスムーズです。入力はNew Input Systemの`Action Map`で定義します。

---

## 4. Phase 2: 敵の進軍＆自動攻撃

**ゴール:** 敵がレーン上を進軍し、兵士が射程内の敵を自動攻撃して倒せる。指揮官にダメージが入る。

### 手順

**Step 2-1: 敵ユニットの実装**

`enemy.py`に敵クラスを作成します。HP・移動速度・攻撃力をパラメータとして持ち、レーンのウェイポイントに沿って移動します。

```python
class Enemy:
    def __init__(self, lane, enemy_type="normal"):
        self.hp = ENEMY_BASE_HP
        self.speed = ENEMY_SPEED
        self.lane = lane
        self.position = lane.spawn_point  # レーン入口
        self.alive = True

    def update(self, dt):
        # ウェイポイントに向かって移動
        self.position += self.direction * self.speed * dt
```

**Step 2-2: 兵士の自動攻撃AI**

`soldier.py`に攻撃ロジックを追加します。一定間隔（`SOLDIER_ATTACK_INTERVAL`）で射程内の最も近い敵に弾を発射します。弾はシンプルな円で描画し、敵に到達したらダメージを与えます。

攻撃の流れ: 射程判定 → クールダウン確認 → 弾生成 → 弾の移動 → 命中判定 → ダメージ適用

**Step 2-3: 敵の消滅とコインドロップ**

敵のHP が0以下になったら消滅アニメーション（フェードアウトまたは点滅）を再生し、コインを加算します。

**Step 2-4: 指揮官へのダメージ**

敵がレーン終端（指揮官の位置）まで到達した場合、指揮官HPを減少させます。HP が0になったらゲームオーバーシーンへ遷移します。

**Step 2-5: 敵のスポーンタイマー**

一定間隔で敵を左右レーンにランダムにスポーンさせる仮のスポーンシステムを作ります（正式なウェーブシステムはPhase 4で実装）。

### 確認チェックリスト

- [ ] 敵がレーン上を進軍する
- [ ] 兵士が自動的に敵を攻撃する
- [ ] 敵が倒されるとコインが加算される
- [ ] 敵が終端到達で指揮官にダメージが入る
- [ ] 指揮官HP=0でゲームオーバーになる

### 所要時間目安: 3～4日

### Unity代替メモ

敵の移動にはNavMeshではなくシンプルなウェイポイント追従（`Vector3.MoveTowards`）で十分です。兵士の攻撃は`Coroutine`で間隔制御し、弾は`Rigidbody2D`を使わず直接座標を動かします。

---

## 5. Phase 3: アイテムシステム実装

**ゴール:** 6種のアイテムをドラッグ＆ドロップでレーンに配置でき、それぞれの効果が発動する。

### 手順

**Step 3-1: アイテムバーUIの作成**

`ui/item_bar.py`で画面下部にアイテムスロットを横並びに配置します。各スロットにはアイコン・残数・クールダウンゲージを表示します。

**Step 3-2: ドラッグ＆ドロップの実装**

Pygameの`MOUSEBUTTONDOWN`/`MOUSEMOTION`/`MOUSEBUTTONUP`イベントで実装します。アイテムスロットをクリックしてドラッグ開始、レーン上でドロップすると配置確定です。配置可能エリアをハイライト表示するとUXが向上します。

```python
# ドラッグ＆ドロップの状態管理
class DragState:
    def __init__(self):
        self.dragging = False
        self.item_type = None
        self.mouse_pos = (0, 0)

    def start(self, item_type, pos):
        self.dragging = True
        self.item_type = item_type
        self.mouse_pos = pos

    def drop(self, lane, position):
        # アイテムを生成してレーンに追加
        self.dragging = False
```

**Step 3-3: 各アイテムの効果実装**

`items/`ディレクトリ以下に各アイテムをクラスとして実装します。共通の基底クラス`Item`を用意し、`activate()`メソッドをオーバーライドします。

| アイテム | 効果 | 実装のポイント |
|---------|------|--------------|
| 増強のポータル | 兵士を追加スポーン | レーンの兵士リストに新規追加、出現アニメーション |
| 攻撃の陣 | 範囲内の兵士の攻撃力UP | バフシステム（持続時間付き）を設計 |
| 重火器キット | 指定兵士の武器変更 | 兵士の攻撃パラメータを一時的に上書き |
| 爆撃要請 | 範囲内の全敵に大ダメージ | エリア判定＋ダメージ適用、画面エフェクト |
| メディカルパック | 指揮官HPを回復 | 即時効果、回復量の上限制御 |
| エナジーシールド | 一定時間ダメージ無効 | 指揮官に無敵フラグ＋持続時間タイマー |

**Step 3-4: コイン消費とクールダウン**

アイテム使用時にコインを消費する処理を追加します。アイテムごとにクールダウンタイマーを設け、連続使用を制限します。コインが足りない場合やクールダウン中は配置をキャンセルし、UIでフィードバックを返します。

### 確認チェックリスト

- [ ] 6種すべてのアイテムがドラッグ＆ドロップで配置できる
- [ ] 各アイテムの効果が正しく発動する
- [ ] コイン不足時に使用が制限される
- [ ] クールダウンがUIに表示される
- [ ] バフの持続時間が正しく管理される

### 所要時間目安: 4～5日

### Unity代替メモ

ドラッグ＆ドロップはUnityの`EventSystem`と`IDragHandler`/`IDropHandler`インターフェースで実装します。アイテム効果はScriptableObjectでデータ駆動にすると拡張しやすくなります。

---

## 6. Phase 4: ウェーブシステム＆ボス戦

**ゴール:** 複数ウェーブの敵出現パターンが機能し、最終ウェーブでボスが登場する。

### 手順

**Step 4-1: ウェーブデータの設計**

`assets/data/waves.json`にウェーブ定義を記述します。JSONデータ駆動にすることで、コードを変更せずにバランス調整が可能になります。

```json
{
  "waves": [
    {
      "wave_number": 1,
      "groups": [
        {
          "enemy_type": "normal",
          "count": 5,
          "lane": "random",
          "spawn_interval": 1.5,
          "delay_from_wave_start": 0
        }
      ]
    },
    {
      "wave_number": 5,
      "groups": [
        {
          "enemy_type": "normal",
          "count": 8,
          "lane": "left",
          "spawn_interval": 1.0,
          "delay_from_wave_start": 0
        },
        {
          "enemy_type": "armored",
          "count": 3,
          "lane": "right",
          "spawn_interval": 2.0,
          "delay_from_wave_start": 5.0
        }
      ]
    },
    {
      "wave_number": 10,
      "is_boss": true,
      "boss_type": "demolisher",
      "groups": [
        {
          "enemy_type": "normal",
          "count": 4,
          "lane": "both",
          "spawn_interval": 2.0,
          "delay_from_wave_start": 3.0
        }
      ]
    }
  ]
}
```

**Step 4-2: ウェーブマネージャーの実装**

`wave.py`でウェーブの進行を管理します。現在のウェーブの全敵を倒すとインターバル（5秒程度）の後に次のウェーブが開始されます。ウェーブ間にアップグレード画面を挟む導線も用意します。

**Step 4-3: 敵バリエーションの追加**

基本の「通常敵」に加え、2～3種の敵タイプを追加します。

- **装甲兵:** HP高め・移動遅め。重火器キットが有効。
- **突撃兵:** HP低め・移動速め。数で押してくる。
- **ヒーラー:** 周囲の敵を回復。優先的に倒す必要がある。

**Step 4-4: ボスの実装**

`boss.py`にボスユニットを実装します。ボスは通常敵と異なり、HPバー表示・複数の攻撃パターン・フェーズ遷移を持ちます。

ボスの行動パターン例:
1. **フェーズ1（HP 100%～50%）:** 通常攻撃＋定期的に雑魚敵を召喚
2. **フェーズ2（HP 50%以下）:** 攻撃速度UP＋範囲攻撃追加

**Step 4-5: 勝利・敗北条件の確定**

ボス撃破で勝利画面、指揮官HP=0で敗北画面に遷移します。リザルト画面にはクリアタイム・獲得コイン・倒した敵数を表示します。

### 確認チェックリスト

- [ ] ウェーブが順番に進行する
- [ ] ウェーブ間にインターバルがある
- [ ] 敵バリエーションが正しく動作する
- [ ] ボスが登場しフェーズ遷移する
- [ ] ボス撃破で勝利、HP=0で敗北になる
- [ ] リザルト画面が表示される

### 所要時間目安: 4～5日

### Unity代替メモ

ウェーブデータはScriptableObjectで管理するのが定番です。ボスの行動パターンはステートマシン（`switch`文で十分）で実装し、Animator Controllerと連携させます。

---

## 7. Phase 5: UI・エフェクト・サウンド

**ゴール:** ゲームの手触りが大幅に向上し、プレイ体験として成立する状態。

### 手順

**Step 5-1: HUDの整備**

`ui/hud.py`に以下を実装します。

- 指揮官HPバー（画面上部、減少時に色が変化）
- コイン表示（アイコン＋数値、獲得時にポップアニメーション）
- ウェーブ表示（「Wave 3/10」形式）
- ボスHPバー（ボス戦時のみ画面上部に大きく表示）

**Step 5-2: ダメージ数値のポップアップ**

敵がダメージを受けた際に数値が浮き上がって消えるエフェクトを実装します。フォントサイズを変えてクリティカル表現も入れられます。

```python
class DamagePopup:
    def __init__(self, position, damage, is_critical=False):
        self.position = list(position)
        self.damage = damage
        self.font_size = 24 if not is_critical else 36
        self.alpha = 255
        self.lifetime = 0.8  # 秒

    def update(self, dt):
        self.position[1] -= 40 * dt   # 上に浮く
        self.alpha -= 255 * dt / self.lifetime
```

**Step 5-3: パーティクルエフェクト**

`effects/particle.py`にシンプルなパーティクルシステムを実装します。主要なエフェクトは以下の通りです。

- 弾の発射時のマズルフラッシュ
- 敵消滅時の破片エフェクト
- 爆撃要請時の爆発エフェクト
- シールド発動時の光輪

**Step 5-4: 画面揺れ（スクリーンシェイク）**

`effects/screen_shake.py`で爆発や大ダメージ時の画面揺れを実装します。描画オフセットをランダムに揺らし、減衰させます。

**Step 5-5: サウンドの実装**

`pygame.mixer`を使い、以下のサウンドカテゴリを実装します。

- **BGM:** ゲームプレイ中のループ曲、ボス戦用曲
- **SE:** 射撃音、敵消滅、アイテム使用、コイン獲得、ダメージ、レーン切り替え
- **ジングル:** ウェーブクリア、ゲームオーバー、勝利

プロトタイプでは無料素材サイト（freesound.org等）から仮素材を入手し、音量バランスだけ調整すれば十分です。

**Step 5-6: タイトル画面**

最低限のタイトル画面を作成します。ゲームタイトルロゴ＋「START」ボタン＋BGMがあればプレゼンテーションに使えます。

### 確認チェックリスト

- [ ] HPバー・コイン・ウェーブがHUDに表示される
- [ ] ダメージ数値がポップアップする
- [ ] 主要アクションにエフェクトがある
- [ ] 画面揺れが機能する
- [ ] BGM・SEが鳴る
- [ ] タイトル画面からゲームを開始できる

### 所要時間目安: 3～4日

### Unity代替メモ

UIはCanvas + TextMeshProで組みます。パーティクルはUnityのParticle Systemが非常に強力なのでそちらを活用してください。サウンドはAudioSourceコンポーネントで管理し、AudioMixerでカテゴリごとの音量制御を行います。

---

## 8. Phase 6: 成長・アップグレードシステム

**ゴール:** ステージクリアで獲得したコインを使い、永続的なアップグレードができる。

### 手順

**Step 6-1: アップグレードデータの設計**

アップグレード項目をJSON等で定義します。各アップグレードにはレベル・コスト・効果量を持たせます。

```python
UPGRADES = {
    "soldier_damage": {
        "name": "兵士攻撃力",
        "max_level": 10,
        "cost_per_level": [100, 200, 350, 500, 700, 1000, 1500, 2000, 3000, 5000],
        "effect_per_level": [2, 2, 3, 3, 4, 4, 5, 5, 6, 8]
    },
    "commander_hp": {
        "name": "指揮官最大HP",
        "max_level": 10,
        "cost_per_level": [80, 160, 300, 450, 600, 900, 1200, 1800, 2500, 4000],
        "effect_per_level": [10, 10, 15, 15, 20, 20, 25, 25, 30, 40]
    },
    "soldier_count": {
        "name": "初期兵士数",
        "max_level": 5,
        "cost_per_level": [200, 500, 1000, 2000, 5000],
        "effect_per_level": [1, 1, 1, 1, 2]
    },
    "coin_bonus": {
        "name": "コイン獲得量",
        "max_level": 5,
        "cost_per_level": [150, 400, 800, 1500, 3000],
        "effect_per_level": [5, 5, 10, 10, 15]  # パーセント
    }
}
```

**Step 6-2: アップグレード画面の作成**

ステージクリア後（またはメニューから）アクセスできるアップグレード画面を作成します。各項目の現在レベル・次レベルのコスト・効果をわかりやすく表示します。

**Step 6-3: セーブ/ロードの実装**

プレイヤーの所持コイン・アップグレードレベル・アンロック済み武器をJSON形式でローカルファイルに保存します。

```python
import json

def save_progress(data, filepath="save.json"):
    with open(filepath, "w") as f:
        json.dump(data, f, indent=2)

def load_progress(filepath="save.json"):
    try:
        with open(filepath, "r") as f:
            return json.load(f)
    except FileNotFoundError:
        return get_default_progress()
```

**Step 6-4: 武器アンロックシステム**

特定条件（累計コイン・ステージクリア回数など）で新しい武器タイプがアンロックされる仕組みを追加します。アンロック時に演出（ファンファーレ＋武器表示）を入れると達成感が出ます。

**Step 6-5: アップグレード効果の適用**

ゲーム開始時にセーブデータを読み込み、アップグレードレベルに応じてパラメータを補正します。`config.py`の基本値に加算する形で実装すると、バランス調整がしやすくなります。

### 確認チェックリスト

- [ ] アップグレード画面が表示される
- [ ] コインを消費してアップグレードできる
- [ ] アップグレード効果がゲーム内に反映される
- [ ] セーブ/ロードが正しく動作する
- [ ] 武器アンロック条件と演出が機能する

### 所要時間目安: 3～4日

### Unity代替メモ

セーブにはUnityの`JsonUtility`または`PlayerPrefs`（少量データ向け）を使います。アップグレードデータはScriptableObjectで管理し、インスペクタから編集できるようにします。

---

## 9. Phase 7: テスト＆バランス調整

**ゴール:** プロトタイプとして人に遊ばせられる品質に仕上げる。

### 手順

**Step 7-1: デバッグモードの実装**

開発中のバランス調整を効率化するデバッグ機能を実装します。

- `F1`: コインを+1000
- `F2`: 現在ウェーブをスキップ
- `F3`: 無敵モード切り替え
- `F4`: 敵スポーン速度2倍/通常切り替え
- `F5`: FPS・エンティティ数の表示切り替え

**Step 7-2: パフォーマンスの最適化**

プロトタイプでもFPS低下は体験を損ねます。以下をチェックします。

- 画面外のエンティティは描画をスキップ
- 死亡した敵・消滅したエフェクトをリストから適切に除去
- 大量のスプライトがある場合は`pygame.sprite.Group`を活用
- パーティクルの同時生成数に上限を設ける

**Step 7-3: 難易度カーブの調整**

バランス調整はゲームの面白さを決定づける最重要工程です。以下の指標を意識して調整します。

- **序盤（Wave 1-3）:** プレイヤーが操作に慣れる区間。敵は弱く少ない。アイテムなしでもクリア可能。
- **中盤（Wave 4-7）:** アイテム使用が必要になる。両レーン同時攻撃が増える。
- **終盤（Wave 8-9）:** リソース管理が問われる。回復・シールドの使いどころが重要。
- **ボス（Wave 10）:** 全力戦。アイテムの最適な使用タイミングが勝敗を分ける。

**Step 7-4: プレイテストの実施**

3～5人の第三者にプレイしてもらい、以下の観点でフィードバックを収集します。

- 操作は直感的か
- 難易度は適切か（簡単すぎ/難しすぎ）
- アイテムの使い分けは楽しいか
- 繰り返しプレイしたくなるか
- どこで詰まったか・困惑したか

**Step 7-5: バグフィックスと最終調整**

プレイテストで見つかった問題を修正し、数値を微調整してプロトタイプを完成させます。

### 確認チェックリスト

- [ ] デバッグモードが実装されている
- [ ] 60FPSが安定している
- [ ] 3人以上のプレイテストを実施した
- [ ] 致命的なバグが修正されている
- [ ] 難易度カーブが「徐々に難しくなる」体験になっている

### 所要時間目安: 3～5日

---

## 10. よくある落とし穴と回避策

### 落とし穴1: クォータービューの座標変換ミス

**症状:** クリック位置とゲーム内座標がずれる、エンティティが予期しない場所に描画される。

**回避策:** ワールド座標⇔スクリーン座標の変換関数を一箇所に集約し、全モジュールから参照させます。変換関数には必ず双方向（`world_to_screen`と`screen_to_world`）を用意してください。デバッグ用にマウス座標のワールド座標表示を常時ONにしておくと問題を早期発見できます。

### 落とし穴2: ドラッグ＆ドロップの判定漏れ

**症状:** アイテムがドロップされない、意図しない場所に配置される。

**回避策:** ドラッグ状態を明確なステートマシンで管理します（IDLE → DRAGGING → DROPPING → IDLE）。マウスアップイベントが画面外で発生した場合のハンドリングも忘れずに実装してください。

### 落とし穴3: 敵の同時大量スポーンによるFPS低下

**症状:** 後半ウェーブでフレームレートが急落する。

**回避策:** オブジェクトプーリング（敵インスタンスの再利用）を導入します。画面上の敵の上限数（例: 50体）を設け、それを超える場合はスポーンをキューイングします。

### 落とし穴4: ゲームバランスの数値設計ミス

**症状:** ゲームが簡単すぎるか難しすぎる、特定アイテムだけが強い。

**回避策:** すべての数値パラメータを`config.py`またはJSONファイルに外出しし、コードに直書きしないでください。スプレッドシートで数値バランスを事前にシミュレーションしてからゲームに反映すると効率的です。

### 落とし穴5: シーン遷移時のリソースリーク

**症状:** ステージを繰り返すとメモリ使用量が増え続ける。

**回避策:** シーン遷移時に全エンティティリスト・タイマー・エフェクトを明示的にクリアする`cleanup()`メソッドを各マネージャクラスに実装してください。

### 落とし穴6: プロトタイプの過剰作り込み

**症状:** 完成度を求めすぎてプロトタイプが完成しない。

**回避策:** プロトタイプの目的は「コアループが面白いか検証すること」です。美麗なグラフィック・豊富なコンテンツ・完璧なUI は後回しにしてください。Phase 4までを最速で完成させ、まずプレイテストに持ち込むことを最優先にしてください。

---

## 11. モバイル移植時の注意点

プロトタイプ完成後にモバイル（iOS / Android）への移植を検討する場合、以下の点に留意してください。

### 11-1. エンジン選定

Pygameはモバイル向けのビルドが困難なため、モバイル移植時にはUnity（C#）またはGodot（GDScript）への移行を推奨します。プロトタイプ段階からモバイルを強く意識するなら、最初からUnityで開発するほうが合理的です。Pygameで検証したゲームデザインとバランス数値はそのまま移行できるため、Pygameプロトタイプは無駄にはなりません。

### 11-2. タッチ操作への変換

| PC操作 | モバイル操作 | 実装メモ |
|--------|------------|---------|
| 左右キーでレーン切り替え | 左右フリック | フリック判定は指の移動量＋速度で判定 |
| アイテムのドラッグ＆ドロップ | そのままタッチD&D | ドラッグ中のアイテムを指の上に表示（指で隠れないよう少し上にオフセット） |
| マウスホバー | ロングプレス | アイテム説明のツールチップ表示に使用 |

### 11-3. 画面解像度とレイアウト

- **基準解像度:** 1920×1080をベースにスケーリングすると多数のデバイスに対応しやすい
- **セーフエリア:** ノッチ・パンチホール・ナビゲーションバーの領域を避けてUIを配置
- **アスペクト比:** 16:9～20:9の範囲に対応。レーンの表示領域は固定比率にし、余白は背景で吸収
- **UI要素の最小サイズ:** タップ対象は最低44×44ポイント（Apple HIG準拠）

### 11-4. パフォーマンス

- **目標FPS:** 30fps安定（60fps推奨だがバッテリー消費とトレードオフ）
- **描画最適化:** スプライトアトラスでドローコールを削減
- **メモリ:** テクスチャサイズに注意（512×512以下推奨）。ローエンド端末のRAMは2GB程度
- **バッテリー:** 不要な演算を削減。バックグラウンド時はゲームを一時停止

### 11-5. モバイル特有の機能

- **中断/復帰:** ホームボタン・電話着信時の自動ポーズ
- **セーブ:** アプリが強制終了されても進捗が失われないよう、ウェーブ間で自動セーブ
- **通知:** （将来的に）体力回復や限定イベント通知

---

## 12. 全体スケジュール概観

| フェーズ | 内容 | 目安 | 累計 |
|---------|------|------|------|
| Phase 1 | 基本レーン＆キャラクター表示 | 2～3日 | 2～3日 |
| Phase 2 | 敵の進軍＆自動攻撃 | 3～4日 | 5～7日 |
| Phase 3 | アイテムシステム実装 | 4～5日 | 9～12日 |
| Phase 4 | ウェーブシステム＆ボス戦 | 4～5日 | 13～17日 |
| Phase 5 | UI・エフェクト・サウンド | 3～4日 | 16～21日 |
| Phase 6 | 成長・アップグレードシステム | 3～4日 | 19～25日 |
| Phase 7 | テスト＆バランス調整 | 3～5日 | 22～30日 |

**合計: 約3～4週間**（1日4～6時間の作業想定）

Phase 4まで（約2.5週間）でデモ可能な最小プロトタイプが完成します。まずはここを目標にしてください。

---

## 付録: 推奨フリー素材サイト

- **スプライト:** kenney.nl（CC0）、opengameart.org
- **サウンド:** freesound.org、kenney.nl
- **フォント:** Google Fonts（Noto Sans JP 推奨）
- **マップタイル:** kenney.nl の Isometric シリーズ

---

> **最終更新:** 2026-05-04
> **本ガイドはプロトタイプ作成用です。** 製品版開発では別途、アセットパイプライン・CI/CD・アナリティクスなどの設計が必要になります。
